# بناء وحدة جديدة — دليل سريع

كل وحدة تكاملية جديدة تحمل **طبقة تجربة المعلمة** تلقائيًّا (شريط علوي · بحث · وضع تدريس · مؤقّت مراحل · متابعة إنجاز · خريطة رحلة · أيقونات · عمل دون اتصال/PWA · طباعة) دون أي إعداد إضافي.

## الطريقة الأسرع

انسخي **`unit-template.html`** باسم وحدتك (مثلًا `unit-makhlouq.html`)، ثم املئي المحتوى. القالب مُهيّأ بالكامل.

أو انسخي وحدة قائمة نموذجًا: `unit-layl-nahar.html` (الوحدة النموذجية) أو `unit-family.html`.

## التفعيل التلقائي — كيف يعمل

يكفي **سطر واحد** قبل `</body>`:

```html
<script src="app.js"></script>
```

`app.js` يركّب الباقي بنفسه: يحقن رابط `app.css`، وملف `manifest.json`، و`theme-color`، وأيقونة التطبيق، ويسجّل `sw.js` (العمل دون اتصال) — كلّها بمسارات محسوبة من موقع `app.js` نفسه. لتفادي وميض التنسيق يُفضَّل إبقاء رابط الأنماط ثابتًا في `<head>` أيضًا (موجود في القالب):

```html
<link rel="stylesheet" href="app.css"/>
```

## عقد البنية (لا تغيّري هذه الأسماء)

الطبقة والمجمّع يعتمدان على أصناف ثابتة. حافظي عليها كما هي:

| العنصر | الصنف/السمة المطلوبة |
|---|---|
| الحاوية | `.wrap` |
| عنوان الوحدة | `h1.hero-title` · شعار `.hero-badge-logo img` |
| تبويبات الوحدة | `nav.unitnav` > `button.utab[data-u="KEY"]` |
| لوحة كل تبويب | `.upanel#u-KEY` (المطابقة لـ `data-u`) |
| تبويبات الأيام | `.daytabs` > `button.daytab[data-d="N"]` (بداخله `.d-n` و`.d-t`) |
| لوحة اليوم | `.daypanel#dN` (المطابقة لـ `data-d`) |
| تبويبات المواد | `.subtabs` > `button.subtab[data-s="N"]` (اللون عبر `style="--sc:#..."`) |
| لوحة المادة | `.subpanel[data-s="N"]` > `.day-head` (`.subj` + `h2` + `a.open`) + `.frame-wrap` (`.frame-load` + `iframe[data-src]`) |

- درس كل مادة يُحمَّل كسِلاً عبر `iframe[data-src="lesson-file.html"]` (لا `src` مباشر).
- مفتاح متابعة الإنجاز يُشتقّ تلقائيًّا من اسم الملف: `unit-XXX.html` → `ghars-prog-XXX` (فريد لكل وحدة).

## ملاحظات

- ألوان المواد: كتابي `#2D7A4B` · علّمني `#006B69` · قال رسولي `#6B3F22` · أدب `#55702B` · أحبك `#8660A8` · لساني `#0d6e6e` · مهاراتي `#8B0000`.
- بعد إنشاء الوحدة، فعّلي بطاقتها في `index.html` (استبدلي `div.unit.upcoming` بـ `a.unit` ورابطها).
- الملفّات المشتركة للطبقة: `app.css` · `app.js` · `manifest.json` · `sw.js` (في جذر الموقع). أي تحسين على التجربة يُطبَّق فيها ويسري على كل الوحدات دفعةً واحدة.
- خصوصية عقدية: لا صور/أيقونات لذوات الأرواح، ولا رموز تشبه شعارات دينية، ولا رموز موسيقية.
